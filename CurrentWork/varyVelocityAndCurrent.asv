function res = varyVelocityAndCurrent()
global current;
global velocity;
current = linspace(50,100,5);
velocity = linspace(-1e3,-5e8,5);
focalLength = zeros(length(current),length(velocity));
for i = 1:5
    focalLength(:) = focLengthHelper(i);
end

    
    
contour(velocity,current,focalLength)
end

function aMat = focLengthHelper(anI,current,velocity)
aMat = zeros(1,length(velocity));
    parfor j = 1:length(velocity)
        aMat(j) = paramHelper(current,velocity(j));
    end
end