particleLaunchRadius = .02;
ringRadius = .05;
particleLaunchHeight = .5;
numParticles = 4;
LaunchAnElectron([.02,.02,.5],[0,0,-1e8],ringRadius,500)

theta = linspace(0,2*pi,numParticles);

xParticle = particleLaunchRadius*cos(theta);
yParticle = particleLaunchRadius*sin(theta);
zParticle = particleLaunchHeight*ones(length(x));

for i = 1:length(xParticle)
    LaunchAnElectron([xParticle(i),yParticle(i),zParticle(i)],[0,0,-1e8],ringRadius,50)

hold on
plot3(ans(:,1),ans(:,2),ans(:,3))